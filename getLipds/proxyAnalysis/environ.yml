name: proxy_analysis
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy<2.0
  - pandas
  - scipy
  - scikit-learn
  - pip:
      - fastapi[standard]
      - uvicorn[standard]
      - fastdtw
      - requests
